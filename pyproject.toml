[project]
name = "interventions-project"
version = "0.1.0"
description = "Project for the interventions RL tuning"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.12.0",
    "trl>=0.27.2",
    "vllm>=0.15.1",
]
